spring:
  # DS de desarrollo
  datasource:
    driverClassName: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: MARCELO
    password: 123456
    
  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.Oracle12cDialect
    show-sql : false 
    generate-ddl: false
    hibernate:
      ddl-auto: none
      
  messages:
    encoding: ISO-8859-1
  mvc:
    dispatch-options-request: true
  servlet:
    multipart:
      # Tamano maximo de archivo subido a 20MB
      max-file-size: 20971520
      max-request-size: 23068680
    
server:
#  show-version: true
  servlet:
    context-path: /tfg/api
  port: 8080

---
spring:
    # DS PRE - PRO
  sql:
    init:
      platform: oracle
  datasource:
#    jndi-name: sigpeac.sigpeacOracleDatasource
    driverClassName: 
    urlconnection: 
    u_connection: 
    p_connection:
 
